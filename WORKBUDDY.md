# WorkBuddy entry point

Install the repository skills into `~/.workbuddy/skills`, then invoke `$build-persona-vault`. Use WorkBuddy's available browser capability for interactive login and platform collection, preserve connector checkpoints, and run the shared PersonaVault import and verification commands afterward.

Do not publish target content, credentials, local paths, or personal data.
